{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nvidia.com/schemas/isv-readiness/gaps-0.2.0.schema.json",
  "title": "ISV Readiness Gap Report",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "provider_repo", "domains", "rows"],
  "properties": {
    "schema_version": {
      "const": "0.2.0"
    },
    "provider_repo": {
      "type": "string"
    },
    "domains": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "minItems": 1,
      "uniqueItems": true
    },
    "rows": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/gap_row"
      }
    }
  },
  "$defs": {
    "gap_row": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "domain",
        "step_name",
        "validation_class",
        "requirement_id",
        "labels",
        "status",
        "detection",
        "stage",
        "evidence",
        "remediation",
        "enrichment"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^gap_[0-9a-f]{12}$"
        },
        "domain": {
          "type": "string",
          "minLength": 1
        },
        "step_name": {
          "type": "string",
          "minLength": 1
        },
        "validation_class": {
          "type": ["string", "null"]
        },
        "requirement_id": {
          "type": ["string", "null"]
        },
        "labels": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "uniqueItems": true
        },
        "status": {
          "enum": ["pass", "fail", "not_implemented", "skipped", "error"]
        },
        "detection": {
          "enum": ["static", "dynamic"]
        },
        "stage": {
          "enum": ["coverage", "correctness"]
        },
        "evidence": {
          "$ref": "#/$defs/evidence"
        },
        "remediation": {
          "$ref": "#/$defs/remediation"
        },
        "enrichment": {
          "type": "object",
          "additionalProperties": true
        }
      }
    },
    "evidence": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "message",
        "validation_message",
        "schema_errors",
        "missing_json_fields",
        "stderr_excerpt",
        "script_path",
        "config_path"
      ],
      "properties": {
        "message": {
          "type": "string"
        },
        "validation_message": {
          "type": ["string", "null"]
        },
        "schema_errors": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "missing_json_fields": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "stderr_excerpt": {
          "type": ["string", "null"]
        },
        "script_path": {
          "type": ["string", "null"]
        },
        "config_path": {
          "type": ["string", "null"]
        }
      }
    },
    "remediation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["auto_fixable", "target", "rerun_command", "aws_reference"],
      "properties": {
        "auto_fixable": {
          "type": "boolean"
        },
        "target": {
          "type": ["string", "null"]
        },
        "rerun_command": {
          "type": "string",
          "minLength": 1
        },
        "aws_reference": {
          "type": ["string", "null"]
        }
      }
    }
  }
}
